name: Build and deploy to Azure Container Apps

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      ACR_NAME: suryaacrregistry
      ACR_LOGIN_SERVER: suryamishraacrregistry.azurecr.io
      IMAGE_NAME: personal-site
      RESOURCE_GROUP: personal-site-rg
      CONTAINERAPPS_ENVIRONMENT: surya-container-env
      CONTAINERAPPS_NAME: surya-personal-site

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Log in to ACR
        uses: azure/CLI@v2
        with:
          inlineScript: |
            az acr login --name $ACR_NAME

      - name: Build and push image to ACR
        run: |
          docker build -t $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }} .
          docker push $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }}

      - name: Ensure Container Apps environment exists
        uses: azure/CLI@v2
        with:
          inlineScript: |
            az containerapp env create \
              --name $CONTAINERAPPS_ENVIRONMENT \
              --resource-group $RESOURCE_GROUP \
              --location eastus

      - name: Deploy to Azure Container Apps
        uses: azure/CLI@v2
        with:
          inlineScript: |
            az containerapp up \
              --name $CONTAINERAPPS_NAME \
              --resource-group $RESOURCE_GROUP \
              --environment $CONTAINERAPPS_ENVIRONMENT \az ad sp create-for-rbac --name surya-aca-sp --role contributor --scopes /subscriptions/934d52ad-30d2-4bc6-9e61-a317452609c4/resourceGroups/personal-site-rg --sdk-auth

              --image $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }} \
              --target-port 8000 \a
              --ingress external \
              --registry-server $ACR_LOGIN_SERVER
